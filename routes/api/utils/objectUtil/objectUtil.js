const addMerge = require('./addMerge')
const deepmerge = require('./deepmerge')
const flattenObjectProps = require('./flattenObjectProps')
const removeElementByProperties = require('./removeElementByProperties')
const useStringAsKey = require('./useStringAsKey')

module.exports = {
  addMerge,
  deepmerge,
  flattenObjectProps,
  removeElementByProperties,
  useStringAsKey
}